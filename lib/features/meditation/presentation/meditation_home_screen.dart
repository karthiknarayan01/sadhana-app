import 'package:flutter/material.dart';

/// Placeholder for the meditation duration picker + timer flow — built out
/// in full as its own phase (timer screen, bell/gong audio, drift-backed
/// session persistence). Scaffolded here just to give RootShell a real
/// destination.
class MeditationHomeScreen extends StatelessWidget {
  const MeditationHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Meditation timer — coming soon')),
    );
  }
}
