import 'package:flutter/material.dart';

/// Placeholder for streaks/heatmap/milestones/charts — the explicitly
/// non-negotiable "keep the user motivated" pillar, built out once real
/// session data exists to visualize (see the meditation/breathing phases).
class ProgressScreen extends StatelessWidget {
  const ProgressScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Your progress — coming soon')),
    );
  }
}
